{
  "name": "online-store-root",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}
